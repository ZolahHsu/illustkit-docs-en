---
title:"文件版本"
---
<code>檔案</code> <code>檢視</code>

📌 <code>選單路徑：檔案 \> 文件版本</code>

### 功能

顯示當前文件的 ai 檔版本，例如 2020、cc、cs6…，2020 以上版本統一顯示 illustrator2020。

<Note>
  如將開啟的檔案降存版本，則需要關閉檔案再重開啟，才能顯示正確版本。
</Note>
