---
title: "依短邊尺寸個別縮放"
---
<code>編輯</code> <code>縮放</code> <code>整理</code>

📌 <code>選單路徑：物件 \> 變形 \> 依短邊尺寸個別縮放</code>

### 1. 功能

將短邊縮放到固定長度，可同時縮放橫式與直式的物件。

### 2. 使用時機

* 同時處理橫式與直式物件時。  
* 需要確保物件不低於最低尺寸門檻時。  
* 準備不同長寬比例的素材進行輸出時。  
* 作為輸出前的尺寸整理工具。

### 3. 基本操作方式

將所有物件選取起來，接著在【物件 > 變形】中點選「依短邊尺寸個別縮放」，在跳出的對話框中輸入想要縮放的短邊像素數值。
<img src="/images/scale/obj_demo_scale-ind-short-side_01.gif" width="650"/>  
