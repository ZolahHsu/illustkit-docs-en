---
title: "等寬縮放"
---
<code>編輯</code> <code>縮放</code> <code>整理</code>

📌 <code>選單路徑：物件 \> 變形 \> 等寬縮放</code>

### 1. 功能

可依指定的關鍵物件，將多個選取物件等比縮放為相同寬度；若未指定關鍵物件，則以最上方物件作為參照基準。

### 2. 使用時機

* 需要統一多個物件的寬度時。  
* 製作列表、模組或系統化版面配置，如：宣傳單、菜單、手冊等。  
* 需要以特定物件作為尺寸基準時。  

### 3. 基本操作方式

將所有物件選取起來，按著 Alt / Option 指定關鍵物件，接著在【物件 > 變形】中點選「等寬縮放」。  

<img src="/images/scale/obj_demo_scale-uniform-w_01.gif" width="650"/>  
